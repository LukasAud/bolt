name: Forward PR to bolt-private

on:
  workflow_dispatch:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  forward:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
    - name: Checkout bolt
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Set up variables
      id: vars
      run: |
        echo "pr_number=${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT
        echo "pr_title=${{ github.event.pull_request.title }}" >> $GITHUB_OUTPUT
        echo "pr_branch=forward-pr-${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT

    - name: Download PR patch
      run: |
        curl -sSL -o pr.patch \
          "https://github.com/${{ github.repository }}/pull/${{ steps.vars.outputs.pr_number }}.patch"

    - name: Show patch summary
      run: |
        echo "---- PATCH START ----"
        head -n 40 pr.patch
        echo "---- PATCH END ----"
        

    - name: Clone bolt-private
      run: |
        git clone https://x-access-token:${{ secrets.PRIVATE_REPO_PAT }}@github.com/LukasAud/bolt-private.git ../bolt-private

    - name: Configure Git identity
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
      
    - name: Apply patch to private repo (force even with conflicts)
      run: |
        cd ../bolt-private
        git checkout -b ${{ steps.vars.outputs.pr_branch }}
    
        echo "🔁 Trying to apply patch using git am..."
        git am ../bolt/pr.patch && exit 0
    
        echo "⚠️ git am failed — falling back to manual apply"
    
        # Create any missing target files in the patch
        echo "🔍 Detecting missing files..."
        patch_files=$(grep '^diff --git' ../bolt/pr.patch | awk '{print $3}' | cut -c3-)
        for file in $patch_files; do
          if [ ! -f "$file" ]; then
            echo "📄 Creating placeholder for missing file: $file"
            mkdir -p "$(dirname "$file")"
            touch "$file"
          fi
        done
    
        # Force apply patch with conflict markers (.rej if needed)
        echo "📌 Applying patch with git apply --reject..."
        git apply --reject --whitespace=fix ../bolt/pr.patch || echo "⚠️ Patch had partial conflicts"
    
        git add -A
        git commit -m "Forwarded (with potential conflicts): ${{ steps.vars.outputs.pr_title }}" || echo "⚠️ Nothing to commit"

    - name: Push and create PR
      run: |
        cd ../bolt-private
        git push origin ${{ steps.vars.outputs.pr_branch }}
        gh pr create \
          --title "[Forwarded] ${{ steps.vars.outputs.pr_title }}" \
          --body "Automated forward of https://github.com/${{ github.repository }}/pull/${{ steps.vars.outputs.pr_number }}" \
          --head ${{ steps.vars.outputs.pr_branch }} \
          --base main
      env:
        GH_TOKEN: ${{ secrets.PRIVATE_REPO_PAT }}
